torch>=2.0.0
torchvision>=0.15.0
pandas>=2.0.0
numpy>=1.24.0
faiss-cpu>=1.7.4
Pillow>=9.5.0
matplotlib>=3.7.0
ftfy
regex
tqdm
git+https://github.com/openai/CLIP.git


NOTES:

torch / torchvision → for model loading and tensors.

faiss-cpu → FAISS similarity search (use faiss-gpu if you want GPU acceleration).

pandas, numpy → data handling.

Pillow → image processing (resizing, saving as PNG).

matplotlib → optional (for showing results in notebooks).

ftfy, regex, tqdm → dependencies for CLIP.

The last line installs OpenAI’s CLIP repo directly from GitHub.